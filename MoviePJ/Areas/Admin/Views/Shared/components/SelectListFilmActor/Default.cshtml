<select id="@ViewBag.For" name="@ViewBag.For"
        asp-items="(MultiSelectList)ViewBag.filmActor"
        class="selectpicker form-control" title="Actors list..." multiple ata-live-search="true">
    @if (ViewBag.AllowNull == true)
    {
        var ids = Model.SelectedFilmActor?.Split(',').Select(int.Parse("")).ToList() ?? new List<int>();
        if (ids.Contains(-1))
        {
            <option value="-1" disabled selected>There is no actor</option>
        }
        else
        {
            <option value="-1">There is no actor</option>
        }
    }
</select>